// We use the default Nx mechanism for ensuring the correct environment to
// avoid issues with NODE_ENV such as the ones described here:
// https://glebbahmutov.com/blog/do-not-use-node-env-for-staging/
export const environment = {
  isDevelopment: true,
};
