import { LoginRedirectDirective } from './login-redirect.directive';

describe('LoginRedirectDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginRedirectDirective();
    expect(directive).toBeTruthy();
  });
});
