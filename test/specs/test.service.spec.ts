
import { ComponentFixture, TestBed, inject } from '@angular/core/testing';

describe('testing d3', () => {

    beforeEach(() => {
        TestBed.configureTestingModule({
            providers: [
                
            ]
        });
    });

    it("When parameters are given, builds circular path", () => {
        expect(true).toBe(true);
        
    });
});